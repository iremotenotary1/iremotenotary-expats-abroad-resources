# Property #10 — DigitalOcean App Platform static site template
# DESIGN ONLY. Do not deploy until OWNER / GitHub remote exist and free-tier pricing is confirmed $0/month.
# Region slug is nyc (App Platform), not nyc3 (Spaces).
name: iremotenotary-expats-abroad
region: nyc
services: []
static_sites:
  - name: americans-abroad-latam-gulf
    github:
      repo: OWNER/iremotenotary-expats-abroad-resources
      branch: main
      deploy_on_push: false
    source_dir: /
    output_dir: public
    index_document: index.html
    error_document: 404.html
    environment_slug: html
    # Catchall intentionally omitted — missing routes should return 404.html
